# دليل التثبيت - Mishwar

## المتطلبات الأساسية

- Node.js 18+ 
- npm أو yarn
- Git

## خطوات التثبيت

### 1. تثبيت المتعلقات

```bash
npm install
```

### 2. إعداد قاعدة البيانات

قم بإنشاء ملف `.env` في المجلد الرئيسي:

```bash
cp .env.example .env
```

ثم قم بتعديل ملف `.env` وإضافة:

```
DATABASE_URL="file:./dev.db"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here-change-in-production"
```

**ملاحظة مهمة**: قم بتغيير `NEXTAUTH_SECRET` إلى قيمة عشوائية قوية في الإنتاج.

### 3. إنشاء قاعدة البيانات

```bash
# إنشاء قاعدة البيانات والجداول
npm run db:push

# أو استخدام Prisma Studio لعرض البيانات
npm run db:studio
```

### 4. تشغيل المشروع

```bash
npm run dev
```

افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## البنية الأساسية

### أنواع المستخدمين

1. **الشركات (COMPANY)**: يمكنها البحث عن مواهب وإنشاء مشاريع
2. **المواهب الإبداعية (FREELANCE)**: يمكنها إدارة معرضها وقبول المشاريع

### الميزات الرئيسية

- ✅ نظام المصادقة الكامل
- ✅ البحث والفلترة المتقدمة
- ✅ نظام المطابقة الذكية
- ✅ إدارة المشاريع والتعاون
- ✅ نظام الرسائل والدردشة
- ✅ نظام التقييمات والمراجعات
- ✅ الموارد والتدريب

## الخطوات التالية

1. قم بإنشاء حساب شركة أو موهبة إبداعية
2. استكشف المواهب المتاحة
3. أنشئ مشروع جديد
4. ابدأ التعاون!

## استكشاف الأخطاء

### خطأ في قاعدة البيانات

إذا واجهت مشاكل في قاعدة البيانات:

```bash
# حذف قاعدة البيانات القديمة
rm prisma/dev.db

# إعادة إنشاء قاعدة البيانات
npm run db:push
```

### مشاكل في المصادقة

تأكد من أن `NEXTAUTH_SECRET` موجود في ملف `.env` وقيمة صحيحة.

## الإنتاج

قبل النشر في الإنتاج:

1. قم بتغيير `DATABASE_URL` إلى قاعدة بيانات PostgreSQL أو MySQL
2. قم بتغيير `NEXTAUTH_SECRET` إلى قيمة عشوائية قوية
3. قم بتحديث `NEXTAUTH_URL` إلى رابط موقعك
4. قم بتشغيل `npm run build` للتحقق من أن كل شيء يعمل

